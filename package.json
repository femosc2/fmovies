{
  "name": "fmovies",
  "version": "2.0.0",
  "private": true,
  "description": "Movie catalog — Vue 3 SPA + Vercel serverless API (Firebase Realtime DB)",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "npm run build --prefix client"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2"
  },
  "devDependencies": {
    "@vercel/node": "^5.0.0",
    "typescript": "^5.7.2"
  }
}
